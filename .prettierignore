dist
dist-ssr
node_modules
.wrangler
pnpm-lock.yaml
package-lock.json
src/data/*.json

# Owned and rewritten by release-please on every release. Formatting them here
# would fight the tool, and the same failure would recur each release.
CHANGELOG.md
.release-please-manifest.json

# Goldfish checkpoints, generated by the MCP server and committed as-is.
# Same reasoning as above: formatting them would fight the tool, and every new
# checkpoint would fail format:check until reformatted by hand.
.memories
