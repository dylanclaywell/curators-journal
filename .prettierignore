dist
dist-ssr
node_modules
.wrangler
pnpm-lock.yaml
package-lock.json
src/data/*.json

# Generated by scripts/build-guides.ts, which indents them one space rather than
# two — 204 files of wiki prose that the drift PR asks you to read, where the
# narrower indent keeps a step on one line. Prettier would widen every one of
# them and win the argument again after each regeneration.
public/guides/*.json

# Generated by scripts/build-bosses.ts, indented one space for the same reason:
# 123 drop tables that arrive in the weekly drift PR as a diff someone reads.
public/boss-detail/*.json

# Owned and rewritten by release-please on every release. Formatting them here
# would fight the tool, and the same failure would recur each release.
CHANGELOG.md
.release-please-manifest.json

# Goldfish checkpoints, generated by the MCP server and committed as-is.
# Same reasoning as above: formatting them would fight the tool, and every new
# checkpoint would fail format:check until reformatted by hand.
.memories
